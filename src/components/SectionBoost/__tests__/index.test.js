import "@testing-library/jest-dom";
import { render } from "@testing-library/react";
import SectionBoost from "..";

it("renderiza componente SectionBoost sem erros", () => {
  render(<SectionBoost />);
});
